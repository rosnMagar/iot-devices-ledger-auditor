# C++
storage-core/build/
storage-core/data/*.log

# stale Rust build artifacts (storage-core migrated to C++)
storage-core/target/

# Python
backend-api/__pycache__/
backend-api/.venv/
backend-api/data/*.db
# DATABASE_URL defaults to ./app.db, so anything run from backend-api/ without
# an explicit URL leaves one here.
backend-api/*.db
*.pyc

# Node
node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo
auditor/dist/
auditor/.aws-sam/

# Env
.env
*.env.local

# OS / tooling
.DS_Store
.Rhistory
